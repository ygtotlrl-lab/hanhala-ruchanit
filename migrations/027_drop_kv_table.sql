-- ============================================================================
-- 027_drop_kv_table.sql — טבלת `kv` יורדת מהסכימה
-- ============================================================================
--
-- ⛔ **רצה במסד** — ⚠️ הוחלה בסבב 93 ואומתה: `kv` אינה ב-`information_schema`.
--    ⛔ מיגרציה שכבר רצה אינה נערכת — שינוי נוסף הוא קובץ חדש.
--
-- ⛔⛔ **הסחיפה שנמדדה:** `018` הריצה `delete from public.kv` — ⚠️ **שורות
--    בלבד**, ⛔ ולא `drop table`: ⭐ ובכל זאת נמדד שהטבלה **אינה קיימת**
--    במסד. ⚠️ כלומר היא הופלה מחוץ למיגרציות, ⛔ והריפו המשיך להצהיר
--    `create table if not exists public.kv` בקובץ הסכימה: ⭐ התקנה טרייה
--    הייתה יוצרת טבלה שאיש אינו קורא ממנה ואינו כותב אליה.
--
-- ⚠️ **ואין כאן מחיקת נתון** — ⛔ השורות נמחקו ב-`018` באישור מפורש,
--    ⭐ והטבלה כבר אינה קיימת: `if exists` הופך את הקובץ לאידמפוטנטי,
--    ⚠️ ובמסד החי הוא אינו מוחק דבר.
--
-- ⛔ **והיא משותפת לארבע האפליקציות** — ⚠️ ולכן היא יורדת **כאן בלבד**,
--    ⭐ בריפו של האפליקציה הראשונה בפרויקט: ⛔ עותק שני היה מקור אמת שני.
-- ============================================================================

drop table if exists public.kv;
